# E04-T09 — Padronizar estrutura das páginas React

## Estado e limite

- Aplicar depois da E04-T04.
- Refatoração estrutural sem mudança visual ou regra de negócio.
- Seguir `docs/architecture/TARGET-ARCHITECTURE.md` como fonte normativa.

## Decisões

- Entradas `page.tsx` continuam Server Components e apenas compõem a rota.
- Dieta e Treino compartilham um placeholder porque já existem dois consumidores.
- O placeholder deixa de existir quando as páginas reais de E06/E07 o substituírem.
- Foundations continua como rota técnica, mas somente o exemplo de `Sheet` fica client-side.
- Classes densas saem do JSX para `*.styles.ts`; CSS Module permanece reservado para composição que exija CSS próprio.
- Arquivos de tipos, constantes, utils e hooks só são criados quando possuem conteúdo e responsabilidade reais.

## Ações por arquivo

| Ação | Caminho | Responsabilidade |
| --- | --- | --- |
| CREATE | `src/components/shared/feature-placeholder/feature-placeholder.tsx` | renderizar o estado estrutural compartilhado |
| CREATE | `src/components/shared/feature-placeholder/feature-placeholder.types.ts` | tipar título, descrição e identificação acessível |
| CREATE | `src/components/shared/feature-placeholder/feature-placeholder.styles.ts` | manter classes de composição fora do JSX |
| CREATE | `src/components/shared/feature-placeholder/feature-placeholder.test.tsx` | comprovar contrato e acessibilidade do placeholder |
| EDIT | `src/app/dieta/page.tsx` | compor o placeholder com conteúdo da rota Dieta |
| EDIT | `src/app/treino/page.tsx` | compor o placeholder com conteúdo da rota Treino |
| CREATE | `src/features/foundations/components/foundations-page/foundations-page.tsx` | renderizar a composição técnica estática no servidor |
| CREATE | `src/features/foundations/components/foundations-page/foundations-page.constants.ts` | manter amostras fixas e ordem semântica |
| CREATE | `src/features/foundations/components/foundations-page/foundations-page.styles.ts` | retirar composição densa de classes do JSX |
| CREATE | `src/features/foundations/components/foundations-page/components/foundations-sheet-example.tsx` | conter a única fronteira `"use client"` necessária |
| MOVE | `src/app/foundations/page.test.tsx` para `src/features/foundations/components/foundations-page/foundations-page.test.tsx` | preservar testes estáticos e interativos junto ao componente |
| EDIT | `src/app/foundations/page.tsx` | compor somente `FoundationsPage` no nível da rota |

Não criar `feature-placeholder.constants.ts`, utils, hook ou barrel: o componente
não possui hoje uma responsabilidade que justifique esses arquivos. Não criar
types, utils ou hook vazios para Foundations pelo mesmo motivo.

## Ordem de implementação

1. Criar e testar `FeaturePlaceholder` sem alterar o conteúdo visível.
2. Reduzir Dieta e Treino à composição server-side do componente compartilhado.
3. Extrair constantes e estilos de Foundations.
4. Isolar o `Sheet` e seus imports em `FoundationsSheetExample` com `"use client"`.
5. Tornar a composição principal de Foundations server-side e deixar a rota fina.
6. Mover os testes, adicionar cobertura do placeholder e executar todos os gates.

## Validação obrigatória

- `pnpm run ci`.
- `git diff --check`.
- busca por segredos nos arquivos alterados.
- busca por `"use client"` para confirmar a menor fronteira possível.
- busca por imports de Prisma, dados, application layer e `server-only` nos módulos client.
- validação manual de `/dieta`, `/treino` e `/foundations` por mouse e teclado.
- validação manual em 320 px e zoom de 200%.

## Critérios de aceite

- Rotas finas e server-side.
- Estilos fora do JSX dos componentes extraídos.
- Contratos e constantes separados somente onde há responsabilidade real.
- Conteúdo e nomes acessíveis preservados.
- Nenhuma nova dependência, regra de domínio ou integração de dados.
- Gates locais integralmente verdes.

## Rollback

Reverter somente os arquivos desta refatoração e restaurar as páginas anteriores,
mantendo o shell e o Design System versionado.
